{
  "name": "nurse",
  "version": "1.3.4",
  "description": "Điều Dưỡng — Trợ lý tự động nhập liệu trên VNPT HIS",
  "main": "index.js",
  "directories": {
    "doc": "docs",
    "test": "test"
  },
  "scripts": {
    "test": "node test/no-demo-tab.test.js",
    "build:nurse": "node build/build.js nurse",
    "build:ddt": "node build/build.js ddt",
    "build:all": "node build/release.js",
    "release": "node build/release.js && gh release create v$(node -p \"require('./package.json').version\") dist-zip/*.zip --title \"v$(node -p \"require('./package.json').version\")\" --generate-notes"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "gitnexus": "^1.6.5"
  }
}
